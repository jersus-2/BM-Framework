# BM-Framework
C++ header
## its goood
bm::out is for well out
bm::in is for in
bm::stf ::stg (the space is so the emoji doesn't appear) is for a string
bm::sys ::call (remember) to talk directly to the linix kernel
bm::stf ::nw is basically new key word
bm::stf ::old is for like deleting stuff
bm::enl is \n
bm::mc is for morse code to letter
## shee / bm::stf ::shee 
.find() finds stuff
.open() opens a file(not so litterly)
.write() writes a file
# pro
pro::inout is well in and out so both(<< and >>)
## shee pro edition(pro::stf ::shee )
.find() returns the file
.open() opens the fule (not so litterly ) and you probably can put it in a variable
.write() wtites a file
